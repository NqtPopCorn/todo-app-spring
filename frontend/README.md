# Add shadcn component

```bash
npx shadcn@latest add [component-name]
```

Replace `[component-name]` with the desired component (e.g., `button`, `card`, `dialog`).

# Build ignore unused variable, function

```js
// tsconfig.json
"noUnusedLocals": false,
"noUnusedParameters": false
```
